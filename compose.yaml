include:
  - path: ./postgres/db.yaml
services:
  app:
    build:
      context: app
      target: builder
    ports:
      - "${APP_PORT}:8000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - monitoring

networks:
  monitoring:
    external: true